<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CurrencyMaui.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CurrencyMaui"
    xmlns:views="clr-namespace:CurrencyMaui.Views"
    Shell.FlyoutBehavior="Disabled">

    <!--TabBar>
        <Tab Title="RU">
            <ShellContent ContentTemplate="{DataTemplate views:RuPage}" />
        </Tab>
        <Tab Title="USD">
            <ShellContent ContentTemplate="{DataTemplate views:UsdPage}" />
        </Tab>
        <Tab Title="EUR">
            <ShellContent ContentTemplate="{DataTemplate views:EurPage}" />
        </Tab>
        <Tab Title="Settings">
            <ShellContent ContentTemplate="{DataTemplate views:SettingsPage}" />
        </Tab>
    </TabBar-->

</Shell>
